<% provide(:title, "Activated businesses")%>

<div class="container">
  <h1>Activated Businesses</h1>
  <p class="lead">
    There are <%= pluralize(@count, "activated business") %> in total.
  </p>
  <div class="row">
    <div class="col-md-12">
      <table class="table table-responsive">
        <thead class="thead-inverse">
          <tr>
            <th>#</th>
            <th>Business</th>
            <th>Email</th>
            <th>Agency</th>
            <th>Address</th>
            <th>Requester Name</th>
            <th>Email</th>
            <th>Status</th>
          </tr>
        </thead>
        <% @activated_businesses.each_with_index do |f, index| %>
        <% index += 1%>
        <tbody>
          <tr>
            <th scope="row"> <%= index %></th>
            <td><%= f.business_name %></td>
            <td> <%= f.email %> </td>
            <td> <%= f.agency_type %> </td>
            <td> <%= business_address(f) %> </td>
            <td> <%= f.requester_name %> </td>
            <td> <%= f.requester_email %> </td>
            <td>
              <button class="btn btn-primary">Verified</button>
            </td>
          </tr>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
  <%= paginate @activated_businesses %>
</div>